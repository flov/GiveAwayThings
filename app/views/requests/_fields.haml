=hidden_field_tag 'request[item_id]', params[:id].to_i

- f.fields_for :message do |message|
  =message.hidden_field :title
  =message.hidden_field :author_id
  =message.text_area :text, :size => "40x2"
  %br
