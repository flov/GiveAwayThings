- title "New Messages"

%h2="New Message to #{@message.recipient.username}"

= error_messages_for :messages

- form_for :message, :url => { :action => "create" } do |f|
  = render :partial => 'fields', :locals => { :message => @message, :f => f }

