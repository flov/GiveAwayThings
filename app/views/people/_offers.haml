%h2 Offers:

%table.tablesorter.offer
  %tbody
    -@items_offering.each do |item|
      %tr
        %td.title=link_to(item.title, "/items/#{item.id}-#{item.title.underscore[0..30]}")
        %td since
        %td=time_ago_in_words(item.created_at)
    -@items_offered_and_accepted.each do |item|
      %tr.title
        %td=link_to item.title, item_path(item)
        %td since
        %td=time_ago_in_words(item.created_at)